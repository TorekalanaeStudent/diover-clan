import java.util.*;
public class Gambling {

    Scanner in = new Scanner (System.in);
    int numByUser = 0;

   public String numberGuesser  = "Heisenberg";
    
    public int guessingNumber (Scanner inside) {
    questionPrompt();
    numByUser = inside.nextInt();

    return numByUser;
    }
    public void questionPrompt() {
        System.out.print("Enter the number you want me to guess: ");
    }
    public String showingNumber (int numByUser) {
       return "Your number is :" + numByUser+"Guessed by: "+numberGuesser;
    }

}